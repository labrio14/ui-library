/**
 * Typography primitives — font families, weights and the base scale.
 * Mode-agnostic (type does not change between light and dark).
 */
export const fontFamily = '"Work Sans", sans-serif';

export const fontWeight = {
  regular: 400,
  medium: 500,
  semibold: 600,
  bold: 700,
} as const;
